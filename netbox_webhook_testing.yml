--- 
- 
  connection: network_cli
  gather_facts: false
  hosts: 
    - "{{ device_id }}"
  name: "Netbox Webhook Testing"
  tasks: 
    - 
      dmi01-akron-rtr01: 
        config: 
          - 
            access: 
              vlan: "{{ vlan_id }}"
            name: "{{ interface_id }}"
        state: replaced
      name: "Nxos l2 Interface"
